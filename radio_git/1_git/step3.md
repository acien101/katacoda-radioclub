Y por último, crearemos nuestro primer *commit*.

En git, para añadir un nuevo commit lo haremos así `git commit -m "mi primerito commit"`

Y lo podremos subir a nuestro repositorio remoto con `git push`

¿Puedes?
